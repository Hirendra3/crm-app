# Copy to keystore.properties in this directory (crm-mobile/).
# Paths for storeFile are relative to android/ (Gradle root).
# Example: generate keystore → android/release/office-crm-release.keystore

storePassword=REPLACE_ME
keyPassword=REPLACE_ME
keyAlias=officecrm
storeFile=release/office-crm-release.keystore
